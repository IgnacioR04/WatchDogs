requests>=2.31.0
pandas>=2.1.0
pytest>=7.4.0
google-auth>=2.28.0
google-api-python-client>=2.120.0
pypdf>=4.0.0
yfinance>=0.2.40
pyarrow>=15.0.0
numpy>=1.24.0

# V4 P1.2: permit patch updates within reviewed minor lines while blocking
# unreviewed feature/major upgrades. Legacy requirements above remain unchanged.
fastapi>=0.141.1,<0.142
SQLAlchemy>=2.0.51,<2.1
psycopg[binary]>=3.3.4,<3.4
alembic>=1.19.1,<1.20
pydantic-settings>=2.15.0,<2.16
httpx>=0.28.1,<0.29

# Integration tests use disposable real PostgreSQL instances; Docker is required
# only when those integration tests run. No Redis service/client is introduced.
testcontainers[postgres]>=4.15.0,<4.16
